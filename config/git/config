[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    line-numbers = true

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[ghq]
	root = ~/src

[wt]
	basedir = .worktrees
	copy = .env*
	hook = direnv allow . || true
